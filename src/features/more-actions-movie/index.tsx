import Button from '@/shared/ui/button';

const MoreActionsMovie = () => {
    return (
        <Button
            style={{ maxWidth: '20px' }}
            variant={'contained'}
            size={'large'}
            color={'dark'}
        >
            {`>`}
        </Button>
    );
};
export default MoreActionsMovie;
