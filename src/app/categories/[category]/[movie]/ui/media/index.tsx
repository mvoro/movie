import { MovieMoreInfoType } from '@/entities/movie';
import styles from './media.module.css';

export const Media = (props: MovieMoreInfoType) => {
    return (
        <div className={styles.wrapper}>
            <div />
            <div />
        </div>
    );
};
